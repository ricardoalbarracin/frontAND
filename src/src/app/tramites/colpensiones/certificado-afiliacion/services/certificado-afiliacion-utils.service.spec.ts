/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CertificadoAfiliacionUtilsService } from './certificado-afiliacion-utils.service';

describe('Service: CertificadoAfiliacionUtils', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CertificadoAfiliacionUtilsService]
    });
  });

  it('should ...', inject([CertificadoAfiliacionUtilsService], (service: CertificadoAfiliacionUtilsService) => {
    expect(service).toBeTruthy();
  }));
});

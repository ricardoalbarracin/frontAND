import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VerificarFormComponent } from './verificar-form.component';

describe('VinculacionFormComponent', () => {
  let component: VerificarFormComponent;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
    })
    .compileComponents();
  }));

  beforeEach(() => {
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
